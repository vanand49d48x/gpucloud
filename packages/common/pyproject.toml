[project]
name = "gpucloud-common"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.0.0",
  "pydantic-settings"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
