[project]
name = "gpucloud-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "requests",
  "psutil",
  "docker",
  "pydantic-settings",
  "redis",
  "rq"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
