# GPUCloud Environment Configuration
# Copy this file to .env and fill in your values

# Database Configuration
DATABASE_URL=postgresql://gpucloud:gpucloud@localhost:5432/gpucloud

# JWT Configuration
JWT_SECRET=change-me-to-a-secure-random-string

# AWS Configuration (Required for pod provisioning)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_VPC_ID=vpc-xxxxxxxxx
AWS_SUBNET_ID=subnet-xxxxxxxxx
AWS_SECURITY_GROUP_ID=sg-xxxxxxxxx
AWS_SSH_KEY_NAME=your-ssh-key-name
AWS_INSTANCE_PROFILE=your-instance-profile-name
BASE_AMI_ID=ami-xxxxxxxxx

# Per-Pod IAM and Logging Configuration
AWS_POD_PERMISSIONS_BOUNDARY=arn:aws:iam::YOUR-ACCOUNT-ID:policy/gpucloud-pod-permissions-boundary
ENABLE_PER_POD_IAM=true
ENABLE_PER_POD_LOGGING=true
S3_BUCKET=gpucloud-data  # Optional: S3 bucket for pod data storage

# AWS Catalog Configuration
AWS_CATALOG_REGIONS=["us-east-1"]
MARKUP_MULTIPLIER=1.5
CATALOG_CACHE_TTL_SECONDS=3600

